var chai = require("chai");
var expect = chai.expect;

var TempTest = require('./../routes/tempTest.js');
/*
describe('ExampleTest', function() {
  it('getTest() should return 0 if no items are passed in', function() {
    var exampleTest = new TempTest([]);
    expect(exampleTest.getTest()).to.equal(0);
  });
});
*/
